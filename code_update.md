# 변경 이력 (code_update.md)

| 항목 | 내용 |
|------|------|
| 문서명 | code_update.md |
| 버전 | V1 |
| 날짜 | 2026-06-11 |
| 프로젝트 | AI 주식 분석 시스템 |

---

## 변경 이력

| 날짜 | 시간 | 파일 | 변경 내용 | 이유 |
|------|------|------|-----------|------|
| 2026-06-14 | - | `src/services/boxScanner.js` | 최소 데이터 기준 `750` 고정값 → `Math.floor(periodMonths * 17)` 동적값으로 변경 | 36개월 기간의 실제 거래일은 약 729일로 고정 750 기준 전 종목 탈락 버그. 수정 후 2485개 종목 통과 |
| 2026-06-14 | - | `src/services/aiService.js`, `main.js`, `preload.js`, `src/renderer/index.html`, `src/renderer/renderer.js`, `src/renderer/styles.css` | AI 채팅창 이미지 첨부 비전 기능 추가 | Ollama/Claude 비전 API 활용. 파일 선택/클립보드 붙여넣기(캡처)/드래그 앤 드롭 3가지 방식 지원. base64 변환 후 IPC 전송. 이미지는 DB 미저장(용량). |
| 2026-06-14 | - | `src/renderer/chart.js`, `src/renderer/index.html`, `src/renderer/styles.css` | 차트 2패널→3패널. 최상단 캔들+MA5/20/60 패널 추가 | 이동평균선(MA5 빨강/MA20 노랑/MA60 파랑) 시각화. 박스권 annotation 공유, 팬/윈도우 3패널 동기화. 비율 MA:캔들:지표=2:2:1. |
| 2026-06-14 | - | `src/config/scanner.config.js`(신규), `src/services/boxScanner.js`, `main.js`, `preload.js`, `src/renderer/index.html`, `src/renderer/renderer.js`, `src/renderer/styles.css` | 박스권 스캐너 v4: 설정 파일 분리 + 전체 필터 강화 + UI 설정 창 | config 기반 파라미터화, 유니버스/유동성/최근성/위치/거짓박스권 필터 추가. 사이드바에 설정 창(기본값 표시, 항목별/전체 초기화). localStorage 저장. 2768→2637종목으로 유니버스 정리. |
| 2026-06-14 | - | `src/services/backtest.js`(신규), `main.js`, `preload.js`, `src/renderer/index.html`, `src/renderer/renderer.js`, `src/renderer/styles.css` | 박스권 백테스트 기능 추가 | 최신 스캔 결과 종목별 1000만원 독립 시뮬레이션(3년). 지지선 터치→전액매수, 저항선 터치→전량매도, 손절 없음, 수수료 0.015%. 수익률/승률/평균보유기간/미실현손익 표시. |
| 2026-06-16 | - | `main.js`, `preload.js`, `src/renderer/index.html`, `src/renderer/renderer.js`, `.env.example` | 증분 업데이트 강제 실행 UI + 스트리밍 로그 추가 | 서버의 collector/scripts/incremental.py를 강제 실행(SKIP_NON_BUSINESS_DAY=false). 사이드바 버튼 클릭 → stdout/stderr 스트리밍 로그 표시. COLLECTOR_PYTHON 환경변수로 Python 경로 지정 가능. |
| 2026-06-16 | - | `collector/config.py`, `collector/krx.py`, `.env.example` | KRX 데이터 포털 자동 로그인 추가 | get_market_ohlcv_by_ticker 배치 API는 KRX 로그인 필요. KRX_ID/KRX_PW 환경변수 설정 시 krx.py import 시점에 stock.krx_login() 자동 호출. |
| 2026-06-16 | - | `collector/ohlcv.py` | incremental() 배치→종목별 방식으로 전환 | get_market_ohlcv_by_ticker(배치)는 KRX 로그인 필요로 실패. get_market_ohlcv_by_date(종목별) 기반 _process_tickers 재사용으로 교체. KRX 계정 불필요. 속도 ~28분/일로 증가. |
| 2026-06-16 | - | `main.js`, `preload.js`, `src/renderer/realtrading.js`, `src/renderer/renderer.js` | 부모 창 계좌 요약 패널 데이터 전달 버그 수정 | 실시간 거래 창 로그인 후 계좌 조회 성공 시 메인 창 계좌 패널에 아무 데이터도 표시되지 않던 문제. 차일드→main→메인창 relay 채널(`real:broadcastAccount` / `real:accountUpdated`) 부재가 원인. 4개 파일에 채널 추가 및 `removeRealListeners` 정리. |
| 2026-06-16 | - | `main.js` | 날짜별 로그 파일 저장 + 한글 깨짐 수정 | console.log/warn/error 오버라이드 → logs/YYYY-MM-DD.log에 UTF-8 저장. INFO/WARN은 파일 전용(터미널 출력 억제 — Windows CP949 코드페이지 한글 깨짐 원인). ERROR/CRIT는 파일+stderr 모두 출력. |
| 2026-06-16 | - | `main.js` | 로그 레벨 오분류 2건 수정 | (1) dotenv v17(dotenvx)의 "injected env" startup 메시지를 `{ quiet: true }` 옵션으로 억제. (2) Flask stderr를 무조건 ERROR로 기록하던 문제 → HTTP 접근 로그=INFO, WARNING/Running on=WARN, 실제 오류=ERROR로 패턴 분류. |
| 2026-06-14 | - | `src/services/aiService.js`, `src/renderer/renderer.js`, `src/renderer/styles.css` | 에이전틱 다단계 검색 + 후속 질문 제안 | 인터넷 검색 시 1차(5개)→AI 반성→2차 검색(최대 10개)까지 자동 확장. 답변 끝 [Q:...] 태그 파싱해 클릭 가능 후속 질문 버튼 렌더링. 채팅 스크롤 개선(질문 최상단 고정). |
| 2026-06-21 | - | `src/renderer/realtrading.js`, `src/renderer/realtrading.css`, `src/renderer/renderer.js`, `src/renderer/styles.css` | 보유종목 바 렌더링 + 평균단가(이익분기단가) 표기 | 실시간 거래 창 예수금 아래 보유종목 카드 바 신설(5종목 노출, 초과 시 드래그 가로 스크롤). 카드/메인창 패널 모두 "평균단가(이익분기단가): n,nnn원(n,nnn원)" 포맷. BEP 공식 — 모의: avg×1.0035 (매수수수료 0.35%) / 실투: avg×1.00015/(1-0.00015-0.0018) ≈ avg×1.002101. realBroadcastAccount에 `is_mock` 추가. onRealQuote 수신 시 카드/메인창 패널 in-place 갱신(현재가·평가손익·손익률). 메인창 `pnl_amount` undefined 버그를 (cur-avg)×qty 클라이언트 계산으로 수정. |
| 2026-06-21 | - | `src/renderer/realtrading.js` | 보유종목 자동 구독(A) + 체결 후 계좌 자동 재조회(C) | (A) loadAccount() 성공 시 보유종목 tickers를 realSubscribe 호출 → 관심종목 미등록 보유종목도 현재가/평가손익 tick 갱신. (C) onRealExecution 콜백 끝에 `scheduleAccountRefresh()` 호출 → 부분체결 다수 수신 시 마지막 이벤트 1.5초 후 1회만 loadAccount() 재실행, 수량·평단·BEP·예수금 자동 동기화. TR 호출 폭주 방지를 위한 디바운서 추가. |
| 2026-06-21 | - | `src/db/queries.js`, `main.js`, `preload.js`, `src/renderer/realtrading.html`, `src/renderer/realtrading.css`, `src/renderer/realtrading.js` | 관심종목 추가 — 종목코드/종목명 자동완성 검색 | `searchStocks(query, limit)` 추가: 숫자 입력 시 ticker prefix 매칭, 텍스트 입력 시 ticker/name contains 매칭. ORDER BY로 정확 일치 → ticker prefix → name 순 정렬. `db:searchStocks` IPC + preload `searchStocks(query, limit)` 노출. 입력창에 250ms 디바운스 자동완성 드롭다운 신설 — 종목코드/종목명/거래소 3열 표시. ArrowDown/Up 키보드 네비, Enter 선택, Esc 닫기, 외부 클릭 닫기, mousedown으로 blur 회피. `addWatchlistTicker(ticker?, name?)` 시그니처 확장 — 직접 선택 시 이름 보존, 6자리 숫자 입력 시 DB 보조 조회로 이름 동기화. |
| 2026-06-21 | - | `src/db/queries.js`, `main.js`, `preload.js`, `src/renderer/realtrading.js` | 관심종목 DB 영속화 — `realtime_watchlist` 테이블 활용 (ralph) | 매 세션마다 watchlist 초기화 버그 해결. `getWatchlist()` LEFT JOIN stock_info → ticker+name+market 반환. `addToWatchlist(ticker)` 트랜잭션 + FOR UPDATE로 동시 INSERT 시 display_order 경합 방지, ON DUPLICATE KEY UPDATE로 재추가 시 is_active=1 + display_order 갱신. `removeFromWatchlist(ticker)` DELETE. IPC 핸들러 3개(`db:getWatchlist/addWatchlist/removeWatchlist`) + ticker 형식 검증(`/^[0-9]{6}$/`, 000000 제외). preload 브릿지 `getWatchlist/addWatchlistDB/removeWatchlistDB` 노출. realtrading.js DOMContentLoaded에서 `loadWatchlistFromDB()` 호출 (`checkBridgeStatus` 전 — 로그인 자동 구독에 watchlist 필요), add/remove 시 await + r.success 체크 + 명시 에러 로그. |
| 2026-06-21 | - | `src/db/migrations/009_fix_collation.sql`(신규), `반복실수.md` | MariaDB collation 불일치 fix — 005 테이블 전부 general_ci로 통일 | getWatchlist() LEFT JOIN 시 "Illegal mix of collations (utf8mb4_general_ci,IMPLICIT) and (utf8mb4_unicode_ci,IMPLICIT)" 에러. 마이그레이션 005가 5개 테이블(kiwoom_config, trading_orders, trading_account, realtime_watchlist, user_holdings_realtime)을 unicode_ci로 만들었으나 stock_info 등 기존 테이블은 general_ci. 마이그레이션 009 — `CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci`로 5개 테이블 일괄 변환. initPool() 부팅 시 자동 적용. 반복실수.md에 재발 방지 절차 기록. |
| 2026-06-21 | - | `.env`, `.env.example`, `src/services/aiService.js`, `main.js`, `preload.js`, `src/renderer/index.html`, `src/renderer/renderer.js`, `src/renderer/styles.css`, `src/db/migrations/013_remove_openrouter_engine.sql`(신규), `src/db/init.sql`, `src/db/migrations/001_initial.sql`, `src/db/migrations/006_chat_sessions.sql` | OpenRouter 통합 완전 제거 (롤백) | 사유: free tier rate limit + 인터넷 검색 트리거 시 reflection/본응답/재시도로 호출 폭증 → 분당 한도 즉시 hit. Ollama(일상)/Claude(중요판단) 이중구조 대비 실용성 낮음. (1) `.env`/`.env.example`: `OPENROUTER_*` 4개 키 제거. (2) `aiService.js`: `https` require, `OPENROUTER_HOST`/`OPENROUTER_BASE`/`openrouterHeaders`/`listOpenRouterFreeModels`/`callOpenRouterNonStreaming`/`chatWithOpenRouter`(+429 백오프 재시도 로직) 모두 삭제. reflectForMoreQueries/chat openrouter 분기 제거. historyLimit `40:20`로 원복. exports에서 listOpenRouterFreeModels 제거. (3) `main.js`: import + `openrouter:listModels` IPC 핸들러 제거. (4) `preload.js`: `listOpenRouterModels` 브릿지 제거. (5) `index.html`: OpenRouter 버튼 + select 제거. (6) `renderer.js`: state.engine localStorage 영속 롤백, state.openrouterModel/Models 제거, loadOpenRouterModels/syncEngineModelSelectVisibility 함수 삭제, 엔진 버튼 핸들러 Ollama-only로 원복, sendChat modelArg → state.ollamaModel 직결. (7) `styles.css`: `.engine-btn.openrouter*` 규칙 + 공유 select selector 원복(`#ollama-model-select` 단일, max-width 130px). (8) DB: 마이그레이션 013 — `chat_history WHERE engine='openrouter'` 행 DELETE(응답 보존가치 낮음), `chat_sessions` UPDATE engine='ollama' (세션 메타 보존), `ENUM('ollama','claude','openrouter')` → `ENUM('ollama','claude')` MODIFY. init.sql/001/006도 동기화. (마이그레이션 012는 이력 보존). 4개 JS syntax OK, repo grep `openrouter`/`OpenRouter`/`OPENROUTER` 잔존 = 마이그레이션 012/013 + code_update.md 이력만(정상). |
| 2026-06-21 | - | `src/services/aiService.js` | OpenRouter 429 자동 1회 재시도 + 강화 메시지 | "OpenRouter 429: Provider returned error" 발생. free tier 분당/일별 호출 제한 또는 upstream provider throttle. `chatWithOpenRouter`를 `attempt()` 내부 함수로 분리, 외부 드라이버가 첫 시도 → 429 + 미재시도면 3초 백오프 후 1회 재시도. 재시도 결정은 스트림 시작 전(상태코드 단계)에서만 — onChunk/onDone 미호출이라 클린 재시도. 재실패 시 모델명+권장사항(다른 free 모델 선택)+원본 응답 포함된 안내 메시지 출력. 비-429 에러는 모델명 포함 강화 메시지. |
| 2026-06-21 | - | `src/db/migrations/012_engine_openrouter.sql`(신규), `src/db/init.sql`, `src/db/migrations/001_initial.sql`, `src/db/migrations/006_chat_sessions.sql` | chat_history/chat_sessions engine ENUM에 openrouter 추가 (hotfix) | OpenRouter 통합 후 채팅 시 "Data truncated for column 'engine' at row 1" 에러. 기존 `ENUM('ollama','claude')`로 'openrouter' INSERT 시 truncation. 마이그레이션 012로 `ENUM('ollama','claude','openrouter')` 확장. init.sql + 001_initial + 006_chat_sessions도 신규 설치 대비 동기화. |
| 2026-06-21 | - | `.env`, `.env.example`, `src/services/aiService.js`, `main.js`, `preload.js`, `src/renderer/index.html`, `src/renderer/renderer.js`, `src/renderer/styles.css` | OpenRouter free 모델 통합 (3번째 AI 엔진) — 비전 포함 | 기존 Ollama(기본)+Claude(서브) 이중 구조에 OpenRouter 추가. (1) `.env`/`.env.example`: `OPENROUTER_API_KEY`/`OPENROUTER_DEFAULT_MODEL`/`OPENROUTER_APP_TITLE`/`OPENROUTER_APP_URL` 추가. (2) `aiService.js`: `https` require 추가. `listOpenRouterFreeModels()` — `/api/v1/models`에서 `pricing.prompt && pricing.completion === '0'` 필터, `architecture.modality`/`input_modalities` 기반 `supports_images` 감지. `callOpenRouterNonStreaming()` — 검색 반성용 비스트리밍. `chatWithOpenRouter()` — OpenAI 호환 SSE 스트리밍, 마지막 user 메시지에 비전 첨부 시 `image_url: {url: 'data:base64,...'}` multimodal content 배열로 변환, 비-200 응답은 JSON 에러 본문 표시(rate limit/잘못된 모델/비전 미지원 케이스). `reflectForMoreQueries()` openrouter 분기, `chat()` openrouter 분기, `historyLimit` 30, exports 추가. (3) `main.js`: `listOpenRouterFreeModels` import + `ipcMain.handle('openrouter:listModels')` 추가. (4) `preload.js`: `listOpenRouterModels()` 브릿지. (5) `index.html`: engine-switcher에 `<button data-engine="openrouter">OpenRouter</button>` + `<select id="openrouter-model-select">` 추가. (6) `renderer.js`: `state.engine` localStorage 영속화(last_engine), `state.openrouterModel`/`state.openrouterModels` 추가, `loadOpenRouterModels()` 신규 — 옵션 라벨 `[V] vendor/model:free (NK)` 포맷(V=비전, N=컨텍스트 K), `syncEngineModelSelectVisibility()` 헬퍼로 엔진별 select 토글, 엔진 버튼 핸들러에 openrouter 통합, sendChat 모델 인자 엔진별 분기(ollama→ollamaModel, openrouter→openrouterModel, claude→null). (7) `styles.css`: `.engine-btn.openrouter*` 색상(teal #00bfa5), `#openrouter-model-select` 스타일 공유. |
| 2026-06-21 | - | `src/renderer/index.html`, `src/renderer/styles.css`, `src/renderer/renderer.js` | KR/US 시장 탭 제거 + 통합 사이드바 + US 선택 시 5y 즉시 차트 적용 | 문제 1: 탭 무의미 — `getStockList()`가 SELECT * FROM stock_info (필터 없음)라 KR 사이드바에 US 1만+ 종목 함께 표시됨. 문제 2: US 핀 체크박스 ON 후 차트 미갱신 — `ensureUsOhlcv` finally에 reload 트리거 없음 + prefetchTicker가 5y 적재 전 빈 데이터 캐싱. **변경**: (1) `index.html`: `.market-tabs` 버튼 바 + `.market-panel` wrapper(KR/US) 제거. US 검색창(`us-search-wrap`)과 상태(`us-master-status`)를 사이드바 상단으로 흡수. 통합 검색 + 필터에 NASDAQ/NYSE/AMEX 옵션 추가. `<ul id="us-stock-list">` 삭제(통합 `stock-list`로 일원화). (2) `styles.css`: `.market-tabs`/`.market-tab*` 4개 규칙 + `#us-stock-list li` selector 제거. (3) `renderer.js`: `setupMarketTabs` 함수 + 호출 삭제, `state.activeMarketTab` 제거. `renderUsStockList`는 `renderStockList` 호출 wrapper로 축소(state.usStocks는 lookup 용도로 보존). `renderStockList` 항목에 `state.usRegistering` 기반 us-spinner 추가. 통합 `stock-list` click 핸들러에 US ticker 핀 ON 분기(usMeta lookup → `ensureUsOhlcv`). `ensureUsOhlcv` finally에 prefetchCache.delete + loadStockList + loadUsStockList + renderTickerTabs + `state.ticker === ticker면 loadStockData 강제(selectTicker 단락 회피)` / 아니면 selectTicker. `handleUsSearchSelect` 흐름을 단순화 — togglePin 자동 ON + ensureUsOhlcv 위임(중복 제거). |
| 2026-06-21 | - | `src/renderer/renderer.js` | US 사이드바 핀 체크박스 + 탭 노출 + 5y 자동 적재 | 문제: US 종목 리스트에 KR과 달리 `pin-checkbox` 미렌더 → 핀 불가 → 상단 탭 미노출. (1) `renderUsStockList` `<li>`에 `<input class="pin-checkbox">` 추가(KR 라인 410과 동일 구조, title="핀(탭 노출 + 5년치 적재)"). (2) `us-stock-list` click 핸들러에 체크박스 분기 추가(`togglePin` + 핀 ON 시 `ensureUsOhlcv` 호출). (3) `ensureUsOhlcv(ticker, name, market)` 헬퍼 신규 — `usRegisterStock` IPC(=init 모드 fetch_ohlcv_5y, INSERT IGNORE)를 idempotent 호출, stock_info만 있고 stock_daily 비었을 가능성 차단. (4) `renderTickerTabs` lookup을 `state.allStocks + state.usStocks` 병합 Map으로 변경 → US ticker pin 시 탭 이름 정상 표시. (5) tab-close 시 `.pin-checkbox` selector(line 511)는 KR/US 공통 클래스 → 자동 동기화. `selectTicker`+`prefetchTicker`+`loadStockData`(`setChartCurrency` 통화 분기)는 ticker-agnostic이라 변경 불필요. |
| 2026-06-22 | - | `infra/perplexica/*`, `infra/openwebui/docker-compose.yml`, `infra/GUIDE.md` | Ollama 호스트 확정 — 30서버(192.168.20.30:11434)로 통일 | 사용자 정정: Perplexica + Open WebUI 모두 30서버 Ollama 사용. host.docker.internal extra_hosts 매핑 제거 → LAN IP 직접 호출(`192.168.20.30:11434`). config.toml.example `[MODELS.OLLAMA] API_URL` 변경. GUIDE.md 인프라 다이어그램에 30/80 서버 분리 + curl 테스트 명령 30서버 IP로 갱신. |
| 2026-06-22 | - | `infra/perplexica/docker-compose.yml`(신규), `infra/perplexica/config.toml.example`(신규), `infra/openwebui/docker-compose.yml`(신규), `infra/GUIDE.md`(신규), `src/services/perplexicaClient.js`(신규), `src/services/aiService.js`, `.env`, `.env.example` | Perplexica 80서버 배포 + aiService 옵션 B 통합 + Open WebUI 별도 배포 | 사용자 결정: SearXNG snippet 한계 보완 위해 Perplexica(답변 엔진) 80서버 배포 + stock-talk aiService 옵션 B 통합. 별도로 80서버에 Open WebUI 구축(가족/팀 내부 사용, stock-talk 비의존). **변경**: (a) `infra/perplexica/docker-compose.yml` — Perplexica 컨테이너(포트 3000) + 기존 SearXNG :8888 재사용 + Ollama host.docker.internal 연결. (b) `infra/perplexica/config.toml.example` — LLM provider API_KEY placeholder + SearXNG URL 명시. (c) `infra/openwebui/docker-compose.yml` — Open WebUI(포트 8080) + WEBUI_AUTH 활성 + SearXNG 웹검색 통합. (d) `infra/GUIDE.md` — 배포 순서/헬스체크/방화벽/백업/문제해결 종합. (e) `src/services/perplexicaClient.js`(신규) — POST /api/search 호출, focusMode=webSearch, optimizationMode=balanced 기본. 60초 타임아웃(LLM 추론 포함). 실패 시 throw 안 함. (f) `aiService.js`: perplexicaClient import. chat() "인터넷" 트리거 시 Perplexica 1순위 호출 → 성공 시 답변+출처 buildSystemPrompt로 전달. 실패 시 기존 SearXNG 다단계 검색 폴백. (g) buildSystemPrompt F블록 재작성 — perplexicaResult 있으면 "Perplexica 1차 답변 + 출처 본문 발췌" + "박스권/모드/holdings와 결합해 최종 답변 생성하라" 지시문. SearXNG 폴백 시 기존 snippet F블록. (h) `.env`/`.env.example` PERPLEXICA_URL 추가. **옵션 B 핵심**: Perplexica가 1차 답변 + 출처 본문까지 제공 → 우리 LLM이 박스권/모드/holdings 컨텍스트와 결합해 최종 답변. SearXNG snippet only 한계(가격 캐시, 시점 불명) + 본문 미수집 한계 동시 해소. |
| 2026-06-22 | - | `src/renderer/realtrading.html`, `src/renderer/realtrading.js`, `src/renderer/index.html`, `src/renderer/renderer.js` | 예수금 → 매매가능금액 표시 전환 | 사용자 관찰: 예수금 표시는 매수 후에도 차감 안 됨(매도 시점에만 변동) → 매매 가능 잔액 파악 불가. 예: 예수금 2990만원, 안랩 500만원 매수 후에도 예수금 2990만원 그대로, 실제 매매가능 2490만원. **변경**: OPW00001 응답의 `주문가능금액`(=orderable, bridge.py 이미 응답에 포함됨) 우선 표시. 실시간 거래 창 + 메인 창 계좌 패널 양쪽 라벨 "예수금" → "매매가능"으로 변경, 값 소스 `account.deposit` → `account.orderable != null ? account.orderable : account.deposit` 폴백. DOM id(`rt-deposit`/`acct-deposit`)는 호환성 위해 유지. |
| 2026-06-22 | - | `src/services/livePrice.js`(신규), `src/services/aiService.js` | AI 채팅 KR 라이브가 컨텍스트 + SearXNG snippet 가격 마스킹 | 사용자 관찰: SearXNG snippet 가격은 캐시 시점 불명(publishedDate 모든 결과 None). 검증으로 동일 검색에 55,600(6/19 종가) + 58,500(6/17 종가) 동시 등장 확인. AI가 어느 값 채택할지 비결정적. 또 "왜 종가로 확정?" 지적 — DB는 일봉만 보유, 장중 현재가 미반영. 키움 OpenAPI 매번 호출은 채팅창 맥락에서 억지. **해결 옵션 2+3 결합**: (2) 네이버 금융 모바일 API 라이브가 fetch / (3) SearXNG snippet 가격 텍스트 마스킹. **변경**: (a) `src/services/livePrice.js`(신규) — `m.stock.naver.com/api/stock/{ticker}/basic` UTF-8 JSON 호출. 필드: closePrice(장중=현재가), compareToPreviousClosePrice, fluctuationsRatio, marketStatus(OPEN/CLOSE/PRE_OPEN), localTradedAt(ISO 8601). 10초 TTL 메모리 캐시. 5초 타임아웃. 6자리 KR ticker만 허용. 실패 시 null 반환. (b) `aiService.js`: `fetchKoreanLivePrice` import. `searchSearXNG` 결과 snippet에 `stripPriceFromSnippet()` 적용 — 한국 가격 패턴 3종(`\d{1,3}(,\d{3})+원?` / `[±▲▼]N.NN%` / `[±]N,NNN원?`) 마스킹 ([가격생략]/[변동률생략]/[가격변동생략]). `chat()`에서 KR 종목이면 라이브가 fetch 후 `buildSystemPrompt`에 `livePrice` 인자 전달. (c) `buildSystemPrompt`: D블록(실시간 지표) 최상단에 `livePrice` 있으면 `라이브 현재가: 54,000원 (-1,600원, -2.88%) [장중]` + `거래 시점: 2026-06-22T13:02:18+09:00 (출처: naver.m.stock.basic)` 표기. 일봉 종가는 "최신 일봉 날짜/일봉 종가"로 라벨 명확화. F블록(검색결과)에 "snippet 가격은 마스킹되었으며 가격은 D블록 라이브가/일봉 종가 사용" 명시. 동작 검증: 2026-06-22 13:02 안랩 54,000원 라이브 응답 확인. 키움 OpenAPI 호출 0회. |
| 2026-06-22 | - | `src/bridge/bridge.py`, `main.js`, `preload.js`, `src/renderer/realtrading.js` | 키움 OnReceiveMsg 추가 + 주문 디버그 강화 + 디바운스 2초 | 사용자 보고 "주문 후 체결정보 미표시". 의심 3가지: (1) 키움 주문 거부 사유 누락 — `OnReceiveMsg` 미구현으로 호가단위 오류/잔고부족/계좌오류 silent fail. (2) OPT10075 응답 cnt 로그 없음 — 응답 비었는지 라이브러리 매핑 실패인지 구분 불가. (3) 주문 디바운스 1초 — 키움 모의서버 처리 전 OPT10075 조회 가능. **변경**: (a) bridge.py: `OnReceiveMsg` 콜백 추가 → SSE `type=message` 이벤트로 모든 키움 서버 메시지 전달. SendOrder 호출 전후 print 로그 강화(accno/ticker/qty/price/hoga/ret). `order` action에서 계좌번호 동적 조회(GetLoginInfo("ACCNO") 우선, env 폴백) — `get_account`와 동일 패턴. SendOrder ret=0 응답에 "요청 전송 완료. 실제 결과는 OPT10075 재조회로 확인" 명시. OPT10075 raw rows / pending / filled 분리 로그 추가. (b) main.js: SSE 핸들러 `message` 이벤트 분기 → 차일드 창에 `real:onMessage` IPC 전달. (c) preload.js: `onRealMessage` 리스너 + `removeRealListeners` 정리. (d) realtrading.js: `onRealMessage` 핸들러 → `showOrderResult('error', ...)` 토스트로 거부 사유 표시. `submitOrder` 디바운스 1000ms → 2000ms (모의서버 처리 시간 확보) + 메시지 "결과는 OPT10075 재조회로 확인 (2초)" 명시. |
| 2026-06-22 | - | `src/bridge/bridge.py`, `src/services/kiwoomService.js`, `main.js`, `preload.js`, `src/renderer/realtrading.js`, `반복실수.md` | 호가 fid 매핑 fix + 체결내역 OPT10075 실데이터 전환 | **#1 호가 표시 오류**: 사용자 보고 "잔량 자리에 가격". 원인 — 브릿지 fid 매핑 오류. `fid 46~50`(잔량으로 사용)이 실제로는 매도호가 6~10, `fid 56~60`(매수잔량으로 사용)이 실제로는 매수호가 6~10. 정확한 키움 fid: 매도잔량 61~65 / 매수잔량 71~75. **수정**: `OnReceiveRealData` 매도 잔량 `60+i`, 매수 잔량 `70+i`. SetRealReg fid_str에 잔량 fid(61~65, 71~75) 추가. **#2 체결내역 OpenAPI 전환**: 사용자 요청 — 메모리 캐시 방식 폐기. 창 닫고 재오픈 시 내역 사라지던 문제. 실시간 관련 데이터는 관심종목 제외 모두 키움 OpenAPI 실데이터 기준. **변경**: (a) bridge.py: `OnReceiveTrData`에 OPT10075 record 캐시 추가(주문번호/원주문번호/종목코드/주문구분/주문상태/주문수량/주문가격/미체결수량/체결가/체결량/체결누계금액/주문시간), `get_executions` action — 체결구분=0(전체) 호출 후 주문상태별 `pending`/`filled` 분리. Flask `/executions` GET 엔드포인트. (b) kiwoomService.js: `getExecutions()` 추가. (c) main.js: `real:getExecutions` IPC. (d) preload.js: `realGetExecutions` 브릿지. (e) realtrading.js: `state.pendingOrders`/`state.execHistory`를 OPT10075 응답 덮어쓰기 전용으로 의미 전환. `loadExecutions()` + `scheduleExecutionsRefresh()` 디바운서 추가. 로그인 직후/체결내역 탭 진입/새로고침 클릭/주문 제출 직후/체결 통보 수신 시 모두 OPT10075 재조회. `submitOrder`의 메모리 임시 행 push 제거. `onRealExecution` 콜백을 알림 신호로만 사용(데이터는 OPT10075로 채움). `cancelPending` localId → orderNo 직접 매칭. 로그아웃 시 두 배열 초기화. **잔여 이슈**: execution SSE 페이로드 키 불일치(`orderNo`/`execQty`/`execPrice` 카멜케이스 vs 렌더러 snake_case 기대)는 이제 미사용(데이터 흐름 자체가 OPT10075로 전환되어 무력화). |
| 2026-06-21 | - | `src/db/migrations/011_expand_stock_name.sql`(신규), `src/db/init.sql`, `src/db/migrations/001_initial.sql`, `collector/yfinance_us.py`, `반복실수.md` | US 수집 정책 재정의 + name 컬럼 확장 | (1) `stock_info.name` VARCHAR(100)→VARCHAR(255) 확장 — US ETF 풀네임이 100자 초과해 `fetch_master` executemany INSERT 시 pymysql DataError(1406) "Data too long for column 'name'" → 마스터 0건 적재 → TQQQ 검색 차단. (2) `fetch_ohlcv_incremental`에서 5y 폴백 제거 — MAX(trade_date) IS NULL 시 0 반환 후 스킵. 마스터 적재 직후 incremental_all_us가 1만+ 종목에 대해 5y 폭주 호출하는 사고 방지(yfinance rate limit, 수십시간 소요). (3) `incremental_all_us` SELECT를 `EXISTS (stock_daily)`로 필터 — 일봉 1행 이상 적재된 종목만 대상(이중 안전). (4) 신규 US 수집 정책: ① 마스터(ticker+name)는 매월 1일 또는 noMaster 시 upfront 적재 ② 5년치 일봉은 관심종목 등록 시점에만 init 모드로 1회 적재 ③ 매일 증분은 일봉 적재된 종목만 ④ 자동 5y 폴백 없음. (5) KR 수집 흐름은 무손 — 변경 없음. |
| 2026-06-21 | - | `src/db/migrations/010_us_stocks.sql`(신규), `src/db/queries.js`, `main.js`, `preload.js`, `src/renderer/index.html`, `src/renderer/styles.css`, `src/renderer/renderer.js`, `src/renderer/chart.js`, `src/renderer/realtrading.js`, `src/services/aiService.js`, `collector/yfinance_us.py`(신규), `collector/scripts/us_sync.py`(신규), `collector/requirements.txt` | Phase A — 미국 주식 분석 도입 (ralph 완료) | (1) DB: stock_info.currency 컬럼(KRW/USD), us_master_sync 메타 테이블, stock_daily FK CASCADE 전환. (2) Python 수집기: yfinance + Nasdaq Trader FTP 마스터(보통주+ETF) + 5y 초기 적재(period='5y') + 종목별 증분(MAX+1)+ FK 방지 _ensure_stock_info. (3) JS IPC: searchStocks marketFilter('KR'|'US'), listStocksByMarket, registerUsStock, getUsMasterSyncedAt, listUsTickers, us:* 5채널 + onUsSyncStatus broadcast. isValidTicker 다중 패턴(KR 6자리/US 1~5자 알파벳). (4) 자동 트리거: app.on('ready')→runUsStartupSync 백그라운드 (첫 부팅 또는 매월 1일 마스터 + 등록 US 종목 증분). 종목당 90초 timeout. (5) UI: 사이드바 KR/US 탭 + 시장 패널 분리. US 패널 — 검색 자동완성(250ms 디바운스) + 등록 마커 ✓ + 등록 진행 스피너 + 마스터 동기화 상태 텍스트. (6) 차트: setChartCurrency 기반 y축 prefix 분기 ($/₩). (7) AI: PROMPT_A_US(Fed FOMC/매크로) + PROMPT_B_US 2모드(US_GENERAL/US_RECOVERY) + buildSystemPrompt currency 분기. US 종목 mode override(평가손실 -10% 이상→RECOVERY). (8) 실시간 거래 창: searchStocks 'KR' 필터 + 알파벳 ticker 입력 차단 alert. 검증: AAPL E2E 1255행, 안랩 회귀 KRW 정상, US/KR 프롬프트 분기 확인. |
|------|------|------|-----------|------|
| 2026-06-11 | 세션 1 | CLAUDE.md | 최초 프로젝트 설계 문서 생성 | 프로젝트 시작 |
| 2026-06-11 | 세션 1 | .claude/agents/*.md | 5개 에이전트 정의 생성 | 서브에이전트 워크플로우 설정 |
| 2026-06-11 | 세션 1 | .claude/advisor_workflow.md | Advisor 워크플로우 생성 | 에스컬레이션 조건 정의 |
| 2026-06-11 | 세션 1 | .claude/hooks/*.sh | 프로젝트 훅 생성 | PostToolUse/SessionEnd 처리 |
| 2026-06-11 | 세션 2 | RESEARCH.md | 최초 생성 — 13섹션 심층 연구 보고서 | /order1 실행, 서브에이전트 3개 병렬 연구 통합 |
| 2026-06-11 | 세션 2 | 차트시각화_기술지표_심층연구_V1_20260611.md | 차트/지표 상세 연구 보조 문서 생성 | 서브에이전트 #2 산출물 |
| 2026-06-11 | 세션 2 | CLAUDE.md | 확정 환경 정보 업데이트 (Ollama 모델, DB 접속 정보, Node 버전) | /order1 확정 정보 반영 |
| 2026-06-11 | 세션 3 | RESEARCH.md | V1→V2 대규모 업데이트 — 섹션 14~19 신규 추가, 기존 섹션 보완 | /order1 재실행, 98% 완성도 목표 |
| 2026-06-11 | 세션 3 | RESEARCH.md | V2 최종 확인 업데이트 — IP 확인, 모델 확인, PyKRX 확정, 체크리스트 완료 | 사용자 5개 항목 확인 답변 반영 |
| 2026-06-11 | 세션 3 | CLAUDE.md | 미결 사항 업데이트, 데이터 소스 확정(PyKRX+yfinance), Ollama 모델 목록 보완 | 사용자 확인 정보 반영 |
| 2026-06-11 | 세션 4 | PLAN.md | 최초 생성 (1670행) — Step 1~9 구현 계획서, 코드 스니펫 포함 | /order2 실행 (98% 완성도 목표) |
| 2026-06-11 | 세션 4 | PLAN.md | V1 완성 (3166행) — Step 2.3 queries.js, Step 6 HTML+CSS, Step 7 Chart.js, Step 8 renderer.js, Step 9 aiService.js 전체 코드 추가 | 컨텍스트 압축 후 재개, 미완성 Step 완성 |
| 2026-06-11 | 세션 4 | PLAN.md | Step 5.4 IPC 핸들러 수정 — LIMIT 방식 → fromDate/toDate Input-based paging | 오프셋 페이징 버그 수정 |
| 2026-06-11 | 세션 5 | main.js | 최종 통합 main.js 생성 — BrowserWindow + 전체 IPC 핸들러 (dialog, db, ai) | PLAN.md Step 1.2+2.2+4.3+8.2+9.2 통합 |
| 2026-06-11 | 세션 5 | src/db/connection.js | mysql2 연결 풀 싱글톤 생성 | Step 2 구현 |
| 2026-06-11 | 세션 5 | src/db/queries.js | DB 쿼리 함수 8개 — Input-based paging 전용 | Step 2 구현 |
| 2026-06-11 | 세션 5 | src/db/init.sql | 4개 테이블 DDL + 안랩 초기 데이터 | Step 3 구현 |
| 2026-06-11 | 세션 5 | src/db/GUIDE.md | DB 폴더 문서화 | Step 2 완성 |
| 2026-06-11 | 세션 5 | src/services/csvImport.js | CSV 파싱 + INSERT IGNORE (100행 배치, 스트림) | Step 4 구현 |
| 2026-06-11 | 세션 5 | src/services/indicators.js | 12개 기술지표 — RSI Wilder/BB N-1 분산 | Step 5 구현 |
| 2026-06-11 | 세션 5 | src/services/aiService.js | Ollama+Claude 이중 구조, 4블록 프롬프트, 6모드 | Step 9 구현 |
| 2026-06-11 | 세션 5 | src/services/GUIDE.md | aiService.js 구현 완료 반영 업데이트 | Step 9 완성 |
| 2026-06-11 | 세션 5 | src/renderer/index.html | 3패널 다크 테마 레이아웃 | Step 6 구현 |
| 2026-06-11 | 세션 5 | src/renderer/styles.css | CSS 변수 기반 다크 테마 | Step 6 구현 |
| 2026-06-11 | 세션 5 | src/renderer/chart.js | Chart.js 3패널 (candlestick+OBV+RSI) + 박스권 어노테이션 | Step 7 구현 |
| 2026-06-11 | 세션 5 | src/renderer/renderer.js | 이벤트 핸들러 + IPC 호출 + AI 스트리밍 | Step 8 구현 |
| 2026-06-11 | 세션 5 | src/renderer/GUIDE.md | renderer 폴더 문서화 | Step 6 완성 |
| 2026-06-11 | 세션 5 | node_modules/ | npm install 완료 (310 packages, 0 vulnerabilities) | 의존성 설치 |
| 2026-06-13 | 세션 6 | DataCollector_V1_20260613.md | 데이터 수집 파이프라인 설계 문서 생성 | /ralph 워크플로우 — 사용자 9개 미결 결정 사항 반영 전 합의 |
| 2026-06-13 | 세션 6 | src/db/migrations/002_stock_info_collector_columns.sql | stock_info 컬럼 확장 (is_active/listed_date/delisted_date/last_synced_at) + idx_is_active 인덱스 | 폐지 종목 추적 + 수집기 마지막 동기화 시각 기록 |
| 2026-06-13 | 세션 6 | collector/__init__.py | 패키지 마커 + __version__ | 서버용 Python 수집기 진입 |
| 2026-06-13 | 세션 6 | collector/config.py | .env 로드 + 환경 상수 노출 + DB 비밀번호 마스킹 summary() | 단일 진입점에서 설정 흡수 |
| 2026-06-13 | 세션 6 | collector/log.py | TimedRotatingFileHandler 30일 + 콘솔 핸들러 | cron stdout/stderr 양쪽 캡처 |
| 2026-06-13 | 세션 6 | collector/notify.py | 텔레그램 sendMessage + graceful skip | 토큰 미설정 시 수집 중단 방지 |
| 2026-06-13 | 세션 6 | collector/db.py | pymysql 싱글톤 + ping reconnect + 트랜잭션 컨텍스트 + executemany | 안정적 DB 적재 + SQL Injection 방지 |
| 2026-06-13 | 세션 6 | collector/krx.py | pykrx 래퍼 — get_all_tickers/get_ohlcv + 3회 지수 백오프 재시도 | KRX 일시 오류 신뢰성 확보 |
| 2026-06-13 | 세션 6 | collector/tickers.py | stock_info 동기화 — 신규/명변경/폐지/복귀 단일 트랜잭션 | 종목 메타 무결성 보존 |
| 2026-06-13 | 세션 6 | collector/ohlcv.py | init_load/incremental/retry_failed + 실패 큐 (state/failed_tickers.json) | 종목별 격리 + 멱등 적재 |
| 2026-06-13 | 세션 6 | collector/scripts/init_load.py | 초기 5년치 일괄 적재 진입점 + 텔레그램 요약 + Exit code 0/1/2 | 1회성 부트스트랩 |
| 2026-06-13 | 세션 6 | collector/scripts/incremental.py | 매일 cron 진입점 — 영업일 체크 + 증분 + 텔레그램 요약 | 16:30 KST 평일 자동 실행 |
| 2026-06-13 | 세션 6 | collector/requirements.txt + .env.example | pykrx/pymysql/dotenv/requests/pandas + 환경 변수 템플릿 | 서버 셋업 명시화 |
| 2026-06-13 | 세션 6 | collector/GUIDE.md + README.md | 폴더 가이드 + 배포 매뉴얼 (텔레그램 봇 등록 / cron / 트러블슈팅) | 운영 표준화 |
| 2026-06-13 | 세션 6 | collector/ohlcv.py | init_load._range() 단순화 — 5년 보장 전제 (INSERT IGNORE로 중복 자동 무시) | cavecrew-reviewer 지적 (혼란스러운 min() 로직 제거) |
| 2026-06-13 | 세션 6 | collector/krx.py | _retry()에 max_attempts 옵션 + get_ticker_name() fast-fail (1회) | 종목명 조회 3,000회 누적 대기 폭증 방지 |
| 2026-06-13 | 세션 6 | collector/requirements.txt | 주석 영문화 (UTF-8 한글 → cp949 충돌 회피) | Windows pip 인코딩 호환 |
| 2026-06-13 | 세션 6 | DB 마이그레이션 실행 | 192.168.20.80 stock_info에 is_active/listed_date/delisted_date/last_synced_at 적용 — pymysql 직접 실행 | 002_stock_info_collector_columns.sql 적용 |
| 2026-06-13 | 세션 6 | collector/krx.py | is_business_day() 요일 기반 단순화 + get_recent_business_day() 폴백 강화 (주말→직전 금요일) | pykrx 미래 날짜 API 응답 불안정 → 정상 영업일에 cron 오스킵 방지 |
| 2026-06-13 | 세션 6 | 안랩(053800) 단일 종목 파이프라인 검증 | OHLCV 30일 조회 + INSERT IGNORE 1행 신규 + DB 누적 3,001행 (2014-03-19 ~ 2026-06-12) 확인 | end-to-end 검증 |
| 2026-06-13 | 세션 6 | collector/krx.py | get_all_tickers_with_names() — FinanceDataReader 우선 + pykrx 폴백 (KOSPI 946+KOSDAQ 1822 일괄 + 종목명 포함) | KRX 메타 endpoint 주말 미응답 회피 + 종목명 조회 N+1 제거 |
| 2026-06-13 | 세션 6 | collector/tickers.py | sync_tickers() — FDR 일괄 결과 직접 사용, 종목명 개별 호출 제거 | 5~10분 → 1초로 단축 |
| 2026-06-13 | 세션 6 | collector/requirements.txt | finance-datareader 추가 | 종목 마스터 폴백 소스 |
| 2026-06-13 | 세션 6 | init_load 백그라운드 실행 (PID 8188) | PowerShell Start-Process detached + RedirectStandardOutput — 2,768 종목 5년치 원격 DB 적재 | 사용자 요청: 메인 프로그램과 별개 백그라운드 |
| 2026-06-13 | 세션 6 | src/renderer/index.html | date-range 기본 selected 3m→6m + 검색 박스(#stock-search) + 시장 필터(#stock-market-filter) + 종목 수 카운트(#stock-count) + 수동 추가 폼 details 접힘 | 사용자 요청: 메인화면 6개월 + 사이드바 검색 |
| 2026-06-13 | 세션 6 | src/renderer/renderer.js | state.fromDate 기본 6m + state.allStocks 캐싱 + renderStockList() 필터 함수 + escapeHtml + debounce(200ms) + 검색/필터 이벤트 | 클라이언트 사이드 검색 (2,768개 메모리 필터) |
| 2026-06-13 | 세션 6 | src/renderer/styles.css | 검색 행 + 시장 필터 + 종목 목록 스크롤(max-height 360px) + 3열 종목 행 (코드/이름/시장) + details summary 스타일 | UI 일관성 |
| 2026-06-13 | 세션 6 | src/renderer/chart.js | updateCharts() 윈도우 캡 제거 — 받은 데이터 전체(fromDate~toDate) 표시. ONE_MONTH_MS 상수 제거 | 사용자 보고: 6m 선택해도 21봉(1개월)만 표시 — 차트가 강제로 최근 1개월로 잘랐던 원인 해결 |
| 2026-06-13 | 세션 6 | src/renderer/chart.js | SIX_MONTH_MS 상수 + 가시 윈도우 6개월 캡 부활 — 데이터가 6개월 초과면 화면은 최근 6개월, 나머지는 드래그 | 사용자 요청: 6개월 캡 + 나머지 드래그 |
| 2026-06-13 | 세션 6 | src/renderer/index.html + renderer.js | 헤더 date-range 기본 6m → 1y, state.fromDate 1년치 로딩 | 화면 6개월 + 추가 6개월치 드래그 영역 확보 |
| 2026-06-13 | 세션 6 | src/renderer/index.html | chart-area 최상단에 #ticker-tabs 영역 추가 | 핀(체크) 종목 탭 표시 자리 |
| 2026-06-13 | 세션 6 | src/renderer/renderer.js | state.pinnedTickers (localStorage 영속) + prefetchCache(TTL 5분) + selectTicker/togglePin/renderTickerTabs/setupTickerTabs/prefetchTicker/prefetchPinned + loadStockData 캐시 우선 + 날짜 변경 시 캐시 무효화 + 핀 재프리로드 | 사용자 요청: 체크박스 핀 + 백그라운드 프리로드 + 탭 UI |
| 2026-06-13 | 세션 6 | src/renderer/styles.css | .pin-checkbox + .ticker-tabs + .ticker-tab (active/cached) + .tab-close 스타일 | 사이드바 체크박스 + 차트 위 탭 바 |
| 2026-06-13 | 세션 6 | src/renderer/index.html | 헤더에 #header-stock-search (datalist 자동완성) + #btn-header-search 추가 | 사용자 보고: 종목 검색 방법 안 보임 — 사이드바 외 헤더에도 큰 검색 박스 |
| 2026-06-13 | 세션 6 | src/renderer/renderer.js | datalist 자동 채움 + resolveHeaderSearch() (정확/시작/포함 매칭 우선순위) + Enter/조회 버튼/datalist 선택 이벤트 | 헤더 검색 동작 — 코드/종목명 입력 → selectTicker() |
| 2026-06-13 | 세션 6 | src/renderer/styles.css | .header-search-wrap + #header-stock-search 220px + #btn-header-search 스타일 | 헤더 검색 UI |
| 2026-06-13 | 세션 6 | src/services/aiService.js + .env | SearXNG 기본 포트 8080 → 8888 (실측 확정 엔드포인트) + 타임아웃 8s → 15s + .env에 SEARXNG_URL 명시 | 사용자 보고: 인터넷 검색 타임아웃 — 포트 오인지 + 타임아웃 부족 |
| 2026-06-13 | 세션 6 | src/renderer/styles.css | #stock-market-filter max-width 70px + #ticker-select max-width 140px | 사용자 요청: 마켓 선택 화면 절반 (사이드바 필터 + 헤더 드롭다운 양쪽 적용) |
| 2026-06-13 | 세션 6 | src/renderer/renderer.js | PINNED_KEY + loadPinnedTickers + savePinnedTickers를 state 선언 *위*로 이동 + catch 로그 추가 | 사용자 보고: 재시작 시 체크 상태 미복원 — state 초기화에서 loadPinnedTickers() 호출 시 PINNED_KEY가 TDZ → ReferenceError가 catch로 흡수되어 항상 빈 배열 반환되던 침묵 버그 수정 |
| 2026-06-14 | 세션 6 | collector/ohlcv.py | incremental() 재작성 — 종목 단위(2,768 호출) → 날짜 단위(영업일당 2 호출, KOSPI+KOSDAQ get_market_ohlcv_by_ticker). MAX(trade_date)+1 ~ today 영업일 루프. 활성 종목 필터 + INSERT IGNORE. 실패 큐 저장 제거(다음 cron 자연 재시도) | 사용자 보고: 증분이 init_load와 동일 시간(~60분) — KRX API 호출 횟수가 병목, 데이터 양이 아님. 변경 후 1 영업일당 ~3초 예상 (1,200배 단축) |
| 2026-06-15 | 세션 9 | src/db/migrations/005_realtime_tables.sql (신규) | 실시간 거래 DB 테이블 5종 추가: kiwoom_config, trading_orders, trading_account, realtime_watchlist, user_holdings_realtime | 3.5단계 실시간 거래 기능 구현 시작 |
| 2026-06-15 | 세션 9 | src/bridge/bridge.py (신규), src/bridge/GUIDE.md (신규) | Python 브릿지 구현: Flask(메인 스레드) + KiwoomWorker(QThread) + SSE 스트림. 엔드포인트 10개. SendOrder 속도제한(1초 5회). OnReceiveChejanData FID 9001(주문번호)/9003(종목코드)/910(체결수량)/911(체결가) | 키움 OpenAPI+ COM 호출은 Python 필수 — Electron에서 직접 호출 불가 |
| 2026-06-15 | 세션 9 | src/services/kiwoomService.js (신규) | Node.js HTTP 래퍼 — Flask 브릿지 HTTP 통신. checkStatus/login/getAccount/getHoldings/orderBuy/orderSell/cancelOrder/subscribe/unsubscribe/shutdown 10개 함수 | Electron main.js IPC 핸들러에서 브릿지 호출 추상화 |
| 2026-06-15 | 세션 9 | main.js | Python 브릿지 spawn(python→py→python3 탐색), 브릿지 준비 폴링(10초), sharedState, createChildWindow, broadcastToAllWindows, SSE 클라이언트(eventsource), real:* IPC 핸들러 14개, 브릿지 자동 재시작(최대 3회) 추가 | 실시간 거래 메인 프로세스 통합 |
| 2026-06-15 | 세션 9 | preload.js | real:* IPC 채널 14개 노출: realOpenWindow, realLogin, realGetAccount, realGetHoldings, realSubscribe, realUnsubscribe, realOrderBuy, realOrderSell, realCancelOrder, realBridgeStatus, onRealQuote, onRealOrderbook, onRealExecution, onRealWindowStateChange, removeRealListeners | 실시간 거래 IPC 브릿지 |
| 2026-06-15 | 세션 9 | src/renderer/realtrading.html, realtrading.js, realtrading.css (신규) | 실시간 거래 차일드 창 UI: 헤더(계좌/예수금), 4탭(관심종목/호가/주문/체결내역), 지정가/시장가, 미체결 취소, 실시간 시세 갱신 | 키움 거래 화면 |
| 2026-06-15 | 세션 9 | src/renderer/index.html, styles.css, renderer.js | [실시간 거래] 버튼 헤더 추가, 계좌 요약 패널(main 우측, 보유종목 현재가 실시간 갱신), setupRealTradingUI(), updateAccountPanel() | 메인 창 실시간 거래 연동 |
| 2026-06-15 | 세션 9 | .env, .env.example | KIWOOM_ACCOUNT_NO/PW/IS_MOCK/BRIDGE_PORT 환경변수 추가 | 브릿지 설정값 |
| 2026-06-15 | 세션 9 | package.json (npm install) | eventsource 패키지 추가 (--legacy-peer-deps) | Node.js main 프로세스에서 SSE 클라이언트 사용 |
| 2026-06-15 | 세션 10 | src/bridge/bridge.py | OPW00001/OPW00004 조회 로직 수정: GetCommData(record='') 버그 → GetCommDataEx로 전환 + 파이프라인 파싱("|" 구분자) | 계좌정보 0원 버그 해결. GetCommData는 record 매개변수가 정확한 TrData 레코드 이름이어야 하는데, pykiwoom이 callback에 빈 문자열을 전달하므로 해결 불가. GetCommDataEx는 record 무관 → 전체 데이터 문자열 반환 가능. OnReceiveTrData 디버그 로그 정리. |
| 2026-06-15 | 세션 10 | src/bridge/bridge.py | 코드 정리: OnEventConnect 보일러플레이트 제거, Flask daemon 스레드 옵션 정리, SendOrder/cancel_order 간결화, 파일 헤더 간단히 | 유지보수성 개선 |
